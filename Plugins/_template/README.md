# PLUGIN_NAME

One-line description of what this plugin does.

## What it does
- ...

## Usage
1. Select one or more nodes.
2. Open the plugin and press Apply.

## Install (dev)
Figma → Plugins → Development → Import plugin from manifest → select `manifest.json`.

## Limitations
- ...

## Files
- `manifest.json` — plugin manifest
- `code.js` — main logic
- `ui.html` — UI (remove if the plugin has no UI; also remove `"ui"` from manifest)
